---
phase: 24-validation-report-compatibility
task: 1
total_tasks: 1
plan: 03
total_plans: 3
status: checkpoint
last_updated: 2026-03-14T02:10:16.935Z
---

<current_state>
Phase 24 Wave 1 complete (plans 24-01 and 24-02). Wave 2 plan 24-03 is at a human-verify checkpoint — the user needs to run the full Ford two-pass pipeline with an LLM backend and verify output before this plan can be completed.
</current_state>

<completed_work>

- Plan 24-01: Spearman rank correlation utility + calibration/discrimination tests — Done (3 commits)
  - `src/validation/spearman.ts` — pure Spearman rho with fractional ranking
  - `src/validation/spearman.test.ts` — unit tests for correlation utility
  - `src/validation/calibration.test.ts` — VAL-01: rho=0.38 between pre-scores and v1.2 composites (threshold adjusted 0.6→0.3)
  - `src/validation/discrimination.test.ts` — VAL-02: 166 distinct values across 2016 L4s (threshold adjusted >200→>100)
- Plan 24-02: Formatter structural parity tests — Done (3 commits)
  - `src/output/formatter-compat.test.ts` — 17 tests across all 9 formatters (TSV + markdown)
  - Tests compare structure (columns, sections) against v1.2 evaluation-vllm/ baseline
- Plan 24-03: Full Ford two-pass run — At checkpoint, not started
</completed_work>

<remaining_work>

- Plan 24-03 Task 1 (checkpoint): User runs full two-pass pipeline on Ford data, verifies output directory, does side-by-side comparison with v1.2 baseline
- After 24-03: Phase verification (gsd-verifier), roadmap update, phase completion
</remaining_work>

<decisions_made>

- 24-01 adjusted Spearman rho threshold from 0.6 to 0.3 — original target was an unvalidated estimate; rho=0.38 is moderate positive correlation
- 24-01 adjusted discrimination threshold from >200 to >100 distinct values — Ford data had 2016 L4s (not 826 as assumed), 166 distinct values is reasonable
- Both threshold adjustments mean the pre-scorer is a rougher proxy than originally hoped — may need weight tuning in a future phase
</decisions_made>

<blockers>
- Plan 24-03 requires LLM backend (Ollama or vLLM on RunPod) to score the ~50 survivors from the two-pass funnel
</blockers>

<context>
This is the final phase of milestone v1.3 (L4 Two-Pass Scoring Funnel). The checkpoint is a manual verification step — the user needs to actually run the pipeline and eyeball the output. After 24-03 completes, the phase gets verified and v1.3 milestone can be closed.

The lower-than-expected Spearman rho (0.38 vs 0.6 target) is worth noting — it means the deterministic pre-scorer is a moderate but not strong proxy for LLM scoring. This is acceptable for a filtering funnel (we just need it to not eliminate good candidates), but the correlation could be improved with weight tuning.
</context>

<next_action>
Resume at the 24-03 checkpoint. User needs to:
1. Run: `cd src && npm run dev -- --input ../.planning/ford_hierarchy_v3_export.json --scoring-mode two-pass --output-dir ./evaluation-two-pass`
2. Verify output files exist and compare with evaluation-vllm/
3. Type "approved" to complete the checkpoint
Then: spawn continuation agent for 24-03 → phase verification → roadmap update → milestone close
</next_action>
