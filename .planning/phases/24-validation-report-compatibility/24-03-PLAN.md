---
phase: 24-validation-report-compatibility
plan: 03
type: execute
wave: 2
depends_on: ["24-01", "24-02"]
files_modified: []
autonomous: false
requirements: [VAL-04]

must_haves:
  truths:
    - "A full two-pass run on Ford 826-candidate data completes successfully and writes output to a new directory"
    - "The two-pass output directory sits alongside evaluation-vllm/ for manual side-by-side comparison"
    - "User has confirmed both output directories contain valid reports"
  artifacts: []
  key_links: []
---

<objective>
Execute a full Ford 826-candidate run in two-pass scoring mode and verify the output alongside the existing v1.2 evaluation-vllm/ baseline for side-by-side comparison.

Purpose: Confirms the entire v1.3 pipeline works end-to-end on real data, producing a complete output directory that can be compared with the v1.2 baseline.
Output: A new evaluation output directory with two-pass results for Ford data.
</objective>

<execution_context>
@/Users/vincent.wicker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincent.wicker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-validation-report-compatibility/24-CONTEXT.md
@.planning/phases/24-validation-report-compatibility/24-RESEARCH.md
@.planning/phases/24-validation-report-compatibility/24-01-SUMMARY.md
@.planning/phases/24-validation-report-compatibility/24-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Full Ford two-pass run and side-by-side verification</name>
  <files></files>
  <action>
    Plans 01 and 02 created permanent validation tests confirming:
    - Spearman rank correlation between pre-scores and v1.2 composites (VAL-01)
    - Score discrimination with more than 200 distinct values (VAL-02)
    - All 10+ formatter structural parity against v1.2 baseline (VAL-03)

    For VAL-04, a full Ford two-pass run is needed with an LLM backend (Ollama or vLLM) to score the ~50 survivors.

    Steps for the user to verify:

    1. Ensure all validation tests pass:
       cd src and npm test

    2. Run the full two-pass pipeline on Ford data:
       Option A (local Ollama):
       cd src and npm run dev -- --input ../.planning/ford_hierarchy_v3_export.json --scoring-mode two-pass --output-dir ./evaluation-two-pass

       Option B (vLLM on RunPod):
       cd src and npm run dev -- --input ../.planning/ford_hierarchy_v3_export.json --scoring-mode two-pass --backend vllm --output-dir ./evaluation-two-pass

    3. Verify the two-pass output directory was created with expected files:
       feasibility-scores.tsv, summary.md, dead-zones.md, meta-reflection.md, tier1-report.md, adoption-risk.md, pre-scores.tsv, simulation-filter.tsv, implementation-shortlist.tsv

    4. Compare side-by-side with v1.2 baseline:
       - Compare TSV column headers between evaluation-vllm/ and evaluation-two-pass/
       - Compare report section headers between both directories
       - Verify pre-scores.tsv exists (two-pass only artifact)
       - Verify scoring mode annotation in report headers

    5. Confirm evaluation-vllm/ directory is untouched (serves as three-lens baseline).

    6. Spot-check report quality: open summary.md from both directories and confirm both are readable, structured reports with similar sections.
  </action>
  <verify>User confirms both output directories contain valid, structurally similar reports</verify>
  <done>Full Ford two-pass run completed. Output directory has all standard report files plus pre-scores.tsv. Side-by-side comparison confirms structural similarity with v1.2 baseline. User approved.</done>
</task>

</tasks>

<verification>
- cd src and npm test -- All validation tests green before the run
- Two-pass output directory exists with all expected report files
- Pre-scores.tsv artifact present (two-pass only)
- Reports include Scoring Mode annotation
- evaluation-vllm/ unchanged (three-lens baseline)
</verification>

<success_criteria>
- Full Ford two-pass run completes without errors
- Output directory has all standard report files plus pre-scores.tsv
- Side-by-side comparison shows structurally similar reports between v1.2 and v1.3
- User confirms both outputs are valid
</success_criteria>

<output>
After completion, create `.planning/phases/24-validation-report-compatibility/24-03-SUMMARY.md`
</output>
