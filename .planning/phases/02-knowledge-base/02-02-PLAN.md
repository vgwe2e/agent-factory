---
phase: 02-knowledge-base
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/process-builder/nodes.json
  - src/data/process-builder/patterns.json
  - src/data/process-builder/fundamentals.md
  - src/data/process-builder/advanced.md
  - src/data/process-builder/nodes-integration.md
  - src/types/knowledge.ts
  - src/knowledge/process-builder.ts
  - src/knowledge/process-builder.test.ts
autonomous: true
requirements:
  - KNOW-02
must_haves:
  truths:
    - "Engine can look up any of the 22 Process Builder nodes by name"
    - "Engine can retrieve the purpose, documentation location, and category for each PB node"
    - "Engine can query workflow patterns (ETL, Decision, Parallel, etc.) with their node compositions"
    - "PB reference data is bundled in the engine, not read from ~/Documents/area at runtime"
  artifacts:
    - path: "src/data/process-builder/nodes.json"
      provides: "Structured node reference for all 22 PB nodes"
      contains: "Process"
    - path: "src/data/process-builder/patterns.json"
      provides: "Common workflow patterns with node compositions"
      contains: "ETL Pattern"
    - path: "src/knowledge/process-builder.ts"
      provides: "Query functions for PB node and pattern lookups"
      exports: ["getPBNode", "getAllPBNodes", "getPBNodesByCategory", "getWorkflowPatterns"]
  key_links:
    - from: "src/knowledge/process-builder.ts"
      to: "src/data/process-builder/nodes.json"
      via: "JSON import at module load time"
      pattern: "import.*from.*data/process-builder"
    - from: "src/knowledge/process-builder.ts"
      to: "src/types/knowledge.ts"
      via: "type imports for PBNode"
      pattern: "import.*PBNode.*from.*types/knowledge"
---

<objective>
Bundle the Aera Process Builder node reference and create a typed query layer for node and pattern lookups.

Purpose: Downstream scoring (Phase 4) needs to know which PB nodes exist to evaluate Aera Platform Fit. Simulation (Phase 6) generates process flows referencing specific PB nodes. This plan extracts structured data from the PB documentation markdown files and wraps them with typed lookup functions.

Output: Structured JSON node reference + workflow patterns in src/data/process-builder/, updated types in src/types/knowledge.ts, query module in src/knowledge/process-builder.ts with tests.
</objective>

<execution_context>
@/Users/vincent.wicker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincent.wicker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Source data location: ~/Documents/area/reference/
- process-builder-index.md (440 lines) - master index with node reference table
- process-builder-fundamentals.md (709 lines) - core concepts, basic nodes
- process-builder-advanced.md (981 lines) - control flow, scripting, data nodes
- process-builder-nodes-integration.md (684 lines) - transaction, workflow, communication nodes

The index contains a "Node Reference Quick Lookup" table with all 22 nodes:

| Node Name | Documentation Location | Primary Purpose |
|-----------|----------------------|-----------------|
| Process | fundamentals.md > Process Node | Entry point, variable definition |
| Interface | fundamentals.md > Using Interface Node | Database/API integration |
| Report Data | advanced.md > Report Data Node | Extract data from reports |
| Data View | advanced.md > Data View Node | Direct access to data without SQL |
| IF | advanced.md > IF Node | Conditional branching |
| While | advanced.md > While Node | Pre-condition loops |
| Do While | advanced.md > Do While Node | Post-condition loops |
| Script | advanced.md > Script Node | Custom Java scripting |
| Transaction | nodes-integration.md > Transaction Node | Atomic transaction groups |
| Rollback Transaction | nodes-integration.md > Rollback Transaction Node | Transaction rollback |
| Wait | nodes-integration.md > Wait Node | Parallel branch coordination |
| Asynchronous | nodes-integration.md > Asynchronous Node | Parallel execution |
| Branch | nodes-integration.md > Branch Node | Process organization |
| Rules | nodes-integration.md > Rules Node | Business rule execution |
| Notification | nodes-integration.md > Notification Node | Email notifications |
| Subprocess | nodes-integration.md > Subprocess Node | Call another process |
| Dynamic Subprocess | nodes-integration.md > Dynamic Subprocess Node | Dynamic process call |
| Stream | nodes-integration.md > Stream Node | Streaming data management |
| Action Item | nodes-integration.md > Action Item Node | Inbox recommendations |
| Data Science | nodes-integration.md > Data Science Node | ML/Analytics integration |
| UI Screen | nodes-integration.md > UI Screen Node | UI integration |
| Agent Team | fundamentals.md > Using Agent Team Node | Agent execution |

The index also contains 7 workflow patterns (ETL, Decision Workflow, Parallel Processing, Iterative Processing, Transactional Update, UI-Driven Process, ML Integration).

Existing project types (from Phase 1): src/types/hierarchy.ts
Plan 02-01 creates src/types/knowledge.ts with PBNode stub type.

IMPORTANT: Plan 02-01 and 02-02 run in parallel (both Wave 1). Plan 02-01 creates src/types/knowledge.ts, so this plan MUST also write to that file. To avoid conflicts, this plan should define its PB-specific types inline or in a separate types file (src/types/process-builder.ts) and the knowledge.ts types will be reconciled after both plans complete. Alternatively, since 02-01 creates the stub PBNode type, this plan can expand it.

RESOLUTION: This plan creates its own src/types/process-builder.ts for PB-specific types. It does NOT modify src/types/knowledge.ts to avoid file conflicts with 02-01. After both plans complete, types can be re-exported from a shared index if needed.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract PB node and pattern data into structured JSON</name>
  <files>src/data/process-builder/nodes.json, src/data/process-builder/patterns.json, src/data/process-builder/fundamentals.md, src/data/process-builder/advanced.md, src/data/process-builder/nodes-integration.md, src/types/process-builder.ts</files>
  <action>
1. Create directory src/data/process-builder/

2. Copy the three PB documentation markdown files as-is into src/data/process-builder/ for full-text reference by the simulation engine (Phase 6 will need the detailed procedures):
   - ~/Documents/area/reference/process-builder-fundamentals.md -> src/data/process-builder/fundamentals.md
   - ~/Documents/area/reference/process-builder-advanced.md -> src/data/process-builder/advanced.md
   - ~/Documents/area/reference/process-builder-nodes-integration.md -> src/data/process-builder/nodes-integration.md

3. Create src/data/process-builder/nodes.json - a structured JSON file with all 22 nodes extracted from the index table. Assign categories based on the index's feature groupings:

```json
{
  "version": "1.0",
  "total_nodes": 22,
  "nodes": [
    {
      "name": "Process",
      "purpose": "Entry point, variable definition",
      "documentation_file": "fundamentals.md",
      "documentation_section": "Process Node",
      "category": "core"
    },
    {
      "name": "Interface",
      "purpose": "Database/API integration",
      "documentation_file": "fundamentals.md",
      "documentation_section": "Using Interface Node",
      "category": "data"
    }
  ]
}
```

Categories to assign (derived from the index's feature groupings):
- "core": Process, Branch
- "data": Interface, Report Data, Data View, Stream
- "control_flow": IF, While, Do While, Script, Rules
- "transaction": Transaction, Rollback Transaction
- "parallel": Wait, Asynchronous
- "orchestration": Subprocess, Dynamic Subprocess
- "communication": Notification, Action Item
- "integration": Data Science, UI Screen, Agent Team

Include all 22 nodes with their exact names from the index table.

4. Create src/data/process-builder/patterns.json - the 7 workflow patterns from the index:

```json
{
  "version": "1.0",
  "patterns": [
    {
      "name": "ETL Pattern",
      "description": "Extract, transform, load data pipeline",
      "node_sequence": ["Data View", "Script", "Interface"],
      "use_case": "Data View Node -> Script Node -> Interface Node (Write)",
      "references": ["advanced.md > Data View Node", "fundamentals.md > Using Interface Node"]
    }
  ]
}
```

Include all 7 patterns: ETL, Decision Workflow, Parallel Processing, Iterative Processing, Transactional Update, UI-Driven Process, ML Integration.

5. Create src/types/process-builder.ts with full types:

```typescript
export type PBNodeCategory = "core" | "data" | "control_flow" | "transaction" | "parallel" | "orchestration" | "communication" | "integration";

export interface PBNode {
  name: string;
  purpose: string;
  documentation_file: string;
  documentation_section: string;
  category: PBNodeCategory;
}

export interface PBNodeIndex {
  version: string;
  total_nodes: number;
  nodes: PBNode[];
}

export interface WorkflowPattern {
  name: string;
  description: string;
  node_sequence: string[];
  use_case: string;
  references: string[];
}

export interface PBPatternIndex {
  version: string;
  patterns: WorkflowPattern[];
}
```
  </action>
  <verify>
    <automated>ls /Users/vincent.wicker/Documents/agent-factory/src/data/process-builder/ && cd /Users/vincent.wicker/Documents/agent-factory/src && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>3 markdown files + 2 JSON files exist in src/data/process-builder/. types/process-builder.ts compiles cleanly. All 22 nodes and 7 patterns are captured in structured JSON.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create PB node query module with tests</name>
  <files>src/knowledge/process-builder.ts, src/knowledge/process-builder.test.ts</files>
  <behavior>
    - Test: getPBNode("Interface") returns PBNode with purpose "Database/API integration" and category "data"
    - Test: getPBNode("nonexistent") returns undefined
    - Test: getPBNode("if") returns the IF node (case-insensitive lookup)
    - Test: getAllPBNodes() returns array of 22 PBNode objects
    - Test: getPBNodesByCategory("control_flow") returns 5 nodes (IF, While, Do While, Script, Rules)
    - Test: getPBNodesByCategory("transaction") returns 2 nodes (Transaction, Rollback Transaction)
    - Test: getWorkflowPatterns() returns array of 7 patterns
    - Test: getWorkflowPattern("ETL Pattern") returns pattern with node_sequence including "Data View"
    - Test: Every node in every pattern's node_sequence exists in getAllPBNodes() (referential integrity)
  </behavior>
  <action>
Create src/knowledge/process-builder.ts:

1. Load nodes.json and patterns.json at module initialization (same approach as components.ts -- JSON import or fs.readFileSync at module level).

2. Build internal Map<string, PBNode> keyed by lowercase node name for case-insensitive lookup.

3. Export query functions:
- `getPBNode(name: string): PBNode | undefined` -- case-insensitive lookup
- `getAllPBNodes(): PBNode[]` -- all 22 nodes
- `getPBNodesByCategory(category: PBNodeCategory): PBNode[]` -- filter by category
- `getWorkflowPatterns(): WorkflowPattern[]` -- all 7 patterns
- `getWorkflowPattern(name: string): WorkflowPattern | undefined` -- lookup by pattern name

4. All functions return typed objects using types from types/process-builder.ts. No `any` types.

Create src/knowledge/process-builder.test.ts using Node.js built-in test runner:
- Write tests for all behaviors listed above
- Verify counts (22 total nodes, 7 patterns, 5 control_flow, 2 transaction)
- Verify referential integrity: every node name in pattern.node_sequence matches a node in getAllPBNodes()
  </action>
  <verify>
    <automated>cd /Users/vincent.wicker/Documents/agent-factory/src && npx tsx --test knowledge/process-builder.test.ts 2>&1</automated>
  </verify>
  <done>All PB query tests pass. getPBNode, getAllPBNodes, getPBNodesByCategory, getWorkflowPatterns return correct typed data. 22 nodes across 8 categories with 7 workflow patterns are queryable. Referential integrity verified.</done>
</task>

</tasks>

<verification>
- `cd src && npx tsc --noEmit` compiles with zero errors
- `cd src && npx tsx --test knowledge/process-builder.test.ts` all tests pass
- `getAllPBNodes().length === 22`
- All 22 node names from the index are present
- All 7 workflow patterns are present with valid node references
- No imports reference ~/Documents/area (data is bundled)
</verification>

<success_criteria>
- 3 PB markdown files bundled for full-text reference
- 22 PB nodes extracted into structured JSON with categories
- 7 workflow patterns extracted into structured JSON
- TypeScript types defined for PB node data structures
- Query functions return typed results for node lookup, category filter, and pattern retrieval
- Referential integrity: all pattern node references resolve to real nodes
</success_criteria>

<output>
After completion, create `.planning/phases/02-knowledge-base/02-02-SUMMARY.md`
</output>
