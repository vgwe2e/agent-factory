---
phase: 08-resilience-recovery
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/infra/checkpoint.ts
  - src/infra/checkpoint.test.ts
  - src/infra/git-commit.ts
  - src/infra/git-commit.test.ts
autonomous: true
requirements:
  - INFR-03
  - INFR-08
must_haves:
  truths:
    - "Engine checkpoints completed opportunity names to a JSON file after each evaluation"
    - "Engine can load a checkpoint and determine which opportunities to skip on resume"
    - "Engine detects stale checkpoints when input file changes"
    - "Engine auto-commits evaluation output directory to git after each cycle"
    - "Git commit failure does not crash the pipeline"
  artifacts:
    - path: "src/infra/checkpoint.ts"
      provides: "Checkpoint save/load/resume filtering"
      exports: ["loadCheckpoint", "saveCheckpoint", "getCompletedNames", "checkpointPath", "Checkpoint", "CheckpointEntry"]
    - path: "src/infra/checkpoint.test.ts"
      provides: "Unit tests for checkpoint"
      min_lines: 50
    - path: "src/infra/git-commit.ts"
      provides: "Auto-commit evaluation artifacts to git"
      exports: ["autoCommitEvaluation", "GitCommitOptions"]
    - path: "src/infra/git-commit.test.ts"
      provides: "Unit tests for git-commit"
      min_lines: 30
  key_links:
    - from: "src/infra/checkpoint.ts"
      to: "node:fs"
      via: "readFileSync/writeFileSync for checkpoint persistence"
      pattern: "import.*node:fs"
    - from: "src/infra/git-commit.ts"
      to: "node:child_process"
      via: "execSync for git commands"
      pattern: "import.*child_process"
---

<objective>
Implement checkpoint persistence for crash recovery and git auto-commit for evaluation artifacts.

Purpose: INFR-08 requires checkpoint/resume so crashed runs skip completed work. INFR-03 requires auto-committing artifacts after each evaluation cycle. These are independent utility modules that will be wired into the pipeline runner (Phase 7).
Output: src/infra/checkpoint.ts and src/infra/git-commit.ts, fully tested.
</objective>

<execution_context>
@/Users/vincent.wicker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincent.wicker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-resilience-recovery/08-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement checkpoint module with TDD</name>
  <files>src/infra/checkpoint.ts, src/infra/checkpoint.test.ts</files>
  <behavior>
    - Test 1: loadCheckpoint returns null when no checkpoint file exists
    - Test 2: saveCheckpoint + loadCheckpoint round-trips checkpoint data correctly
    - Test 3: getCompletedNames returns Set of all completed L3 names from entries
    - Test 4: getCompletedNames returns empty Set when checkpoint is null
    - Test 5: loadCheckpoint returns null when checkpoint file contains invalid JSON (corruption recovery)
    - Test 6: loadCheckpoint returns null when checkpoint fails Zod schema validation
    - Test 7: checkpointPath returns correct path with .checkpoint.json filename
  </behavior>
  <action>
RED: Create src/infra/checkpoint.test.ts with all 7 test cases. Use node:test describe/it and assert/strict. Use mkdtempSync for isolated temp directories in beforeEach, clean up with rmSync in afterEach. Run tests -- all must FAIL.

GREEN: Create src/infra/checkpoint.ts implementing:
- Zod schemas: CheckpointEntrySchema (l3Name: string, completedAt: string, status: enum "scored"|"skipped"|"error"), CheckpointSchema (version: literal(1), inputFile: string, startedAt: string, entries: array of CheckpointEntry)
- Export types: Checkpoint = z.infer of CheckpointSchema, CheckpointEntry = z.infer of CheckpointEntrySchema
- CHECKPOINT_FILENAME = ".checkpoint.json"
- checkpointPath(outputDir: string): string -- joins outputDir + CHECKPOINT_FILENAME
- loadCheckpoint(outputDir: string): Checkpoint | null -- reads file, JSON.parse, safeParse with Zod. Returns null if file missing, JSON invalid, or schema validation fails. No thrown exceptions.
- saveCheckpoint(outputDir: string, checkpoint: Checkpoint): void -- writeFileSync with JSON.stringify indent 2
- getCompletedNames(checkpoint: Checkpoint | null): Set<string> -- returns Set of l3Name from entries, empty Set if null

Use node:fs (readFileSync, writeFileSync, existsSync), node:path (join), and zod. Follow project Result type conventions.

REFACTOR: Run tests -- all must PASS.
  </action>
  <verify>
    <automated>cd /Users/vincent.wicker/Documents/agent-factory/src && npx tsx --test infra/checkpoint.test.ts</automated>
  </verify>
  <done>checkpoint module passes all 7 tests covering normal operation, corruption recovery, and edge cases</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement git auto-commit module with TDD</name>
  <files>src/infra/git-commit.ts, src/infra/git-commit.test.ts</files>
  <behavior>
    - Test 1: Returns committed=false when enabled=false (disabled mode)
    - Test 2: Returns committed=true after staging and committing files in a test git repo
    - Test 3: Returns committed=false with no error when there are no staged changes
    - Test 4: Returns committed=false with error string when not inside a git repo
    - Test 5: Uses custom message when provided, default timestamp message otherwise
  </behavior>
  <action>
RED: Create src/infra/git-commit.test.ts with all 5 test cases. For tests 2-5, create isolated temp directories. For test 2, initialize a git repo (git init, git config user.email/name, create a file, then call autoCommitEvaluation). For test 4, use a temp directory without git init. Run tests -- all must FAIL.

GREEN: Create src/infra/git-commit.ts implementing:
- GitCommitOptions interface: outputDir (string), message (optional string), enabled (optional boolean, default true)
- GitCommitResult type: { committed: boolean; error?: string }
- autoCommitEvaluation(opts: GitCommitOptions): GitCommitResult
  1. If enabled === false, return { committed: false }
  2. Try: execSync "git rev-parse --is-inside-work-tree" with stdio:"pipe"
  3. execSync "git add {outputDir}" with stdio:"pipe"
  4. Check for staged changes: execSync "git diff --cached --quiet" (exit code 1 means changes exist)
  5. If no changes, return { committed: false }
  6. Build message: opts.message or "chore(eval): auto-commit evaluation artifacts {ISO timestamp}"
  7. execSync "git commit -m {message}" with stdio:"pipe"
  8. Return { committed: true }
  9. Catch: return { committed: false, error: message } -- non-fatal

Use child_process.execSync. All git output suppressed with stdio:"pipe". Non-fatal errors -- always returns a result, never throws.

REFACTOR: Run tests -- all must PASS.
  </action>
  <verify>
    <automated>cd /Users/vincent.wicker/Documents/agent-factory/src && npx tsx --test infra/git-commit.test.ts</automated>
  </verify>
  <done>git-commit module passes all 5 tests covering disabled mode, successful commit, no-changes, non-git-repo, and custom messages</done>
</task>

</tasks>

<verification>
cd /Users/vincent.wicker/Documents/agent-factory/src && npm test
All existing tests still pass. checkpoint and git-commit tests pass.
</verification>

<success_criteria>
- checkpoint.ts exports loadCheckpoint, saveCheckpoint, getCompletedNames, checkpointPath
- git-commit.ts exports autoCommitEvaluation
- 12 total unit tests passing across both modules
- No new npm dependencies added
- Checkpoint handles corruption gracefully (returns null, no crash)
- Git failures are non-fatal (returns error string, no crash)
- Existing test suite unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/08-resilience-recovery/08-02-SUMMARY.md`
</output>
