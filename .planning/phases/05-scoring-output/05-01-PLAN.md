---
phase: 05-scoring-output
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/types/scoring.ts
  - src/types/triage.ts
  - src/output/tsv-utils.ts
  - src/output/format-triage-tsv.ts
  - src/output/format-triage-tsv.test.ts
  - src/output/format-scores-tsv.ts
  - src/output/format-scores-tsv.test.ts
autonomous: true
requirements:
  - SCOR-07
  - OUTP-01
  - OUTP-02

must_haves:
  truths:
    - "formatTriageTsv produces tab-separated output sorted by tier ASC then composite DESC"
    - "formatScoresTsv produces tab-separated output with all 9 sub-dimension columns plus lens totals and composite"
    - "Nullable fields render as empty strings, not literal 'null' or 'undefined'"
    - "Both TSV outputs end with a trailing newline"
  artifacts:
    - path: "src/types/scoring.ts"
      provides: "ScoredOpportunity, TechnicalFeasibility, AdoptionRealism, ValueEfficiency, SubDimensionScore, ConfidenceLevel types"
      exports: ["ScoredOpportunity", "TechnicalFeasibility", "AdoptionRealism", "ValueEfficiency", "SubDimensionScore", "ConfidenceLevel"]
    - path: "src/types/triage.ts"
      provides: "TriagedOpportunity, RedFlag, RedFlagType, Tier types"
      exports: ["TriagedOpportunity", "RedFlag", "RedFlagType", "Tier"]
    - path: "src/output/tsv-utils.ts"
      provides: "tsvCell and tsvRow utility functions"
      exports: ["tsvCell", "tsvRow"]
    - path: "src/output/format-triage-tsv.ts"
      provides: "formatTriageTsv pure function"
      exports: ["formatTriageTsv"]
    - path: "src/output/format-scores-tsv.ts"
      provides: "formatScoresTsv pure function"
      exports: ["formatScoresTsv"]
  key_links:
    - from: "src/output/format-triage-tsv.ts"
      to: "src/types/triage.ts"
      via: "import TriagedOpportunity"
      pattern: "import.*TriagedOpportunity.*from.*types/triage"
    - from: "src/output/format-scores-tsv.ts"
      to: "src/types/scoring.ts"
      via: "import ScoredOpportunity"
      pattern: "import.*ScoredOpportunity.*from.*types/scoring"
---

<objective>
Define the shared type contracts for scoring and triage data, then implement the two TSV formatter modules using TDD.

Purpose: Establish the data shape boundary between Phase 3/4 (producers) and Phase 5 (consumer), then build the TSV output formatters that other phases depend on for structured data export.
Output: Type definitions in src/types/ and two pure TSV formatter functions with full test coverage.
</objective>

<execution_context>
@/Users/vincent.wicker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincent.wicker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-scoring-output/05-RESEARCH.md

@src/types/hierarchy.ts
</context>

<interfaces>
<!-- Existing types this plan must reference -->

From src/types/hierarchy.ts:
```typescript
export type LeadArchetype = "DETERMINISTIC" | "AGENTIC" | "GENERATIVE";
export type ImplementationComplexity = "LOW" | "MEDIUM" | "HIGH";
export type FinancialRating = "HIGH" | "MEDIUM" | "LOW";
export type RatingConfidence = "HIGH" | "MEDIUM" | "LOW";

export interface L3Opportunity {
  l3_name: string;
  l2_name: string;
  l1_name: string;
  opportunity_exists: boolean;
  lead_archetype: LeadArchetype | null;
  combined_max_value: number | null;
  quick_win: boolean;
  l4_count: number;
  // ... other fields
}
```

From Phase 4 CONTEXT.md decisions:
- Uniform 0-3 scale for all 9 sub-dimensions
- Lens totals: Technical (0-9), Adoption (0-12), Value (0-6)
- Composite: 0.0-1.0 via weights 0.30/0.45/0.25
- Per-dimension reason strings preserved
- Confidence: HIGH/MEDIUM/LOW, data-driven, overall = lowest lens
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define type contracts and implement triage TSV formatter</name>
  <files>src/types/scoring.ts, src/types/triage.ts, src/output/tsv-utils.ts, src/output/format-triage-tsv.ts, src/output/format-triage-tsv.test.ts</files>
  <behavior>
    - tsvCell(null) returns ""
    - tsvCell(undefined) returns ""
    - tsvCell(true) returns "Y"
    - tsvCell(false) returns "N"
    - tsvCell(42) returns "42"
    - tsvCell("hello") returns "hello"
    - tsvRow(["a", 1, null, true]) returns "a\t1\t\tY"
    - formatTriageTsv([]) returns header row + trailing newline only
    - formatTriageTsv with 3 opps in tiers 3,1,2 produces rows sorted as tier 1, 2, 3
    - Within same tier, opportunities sorted by combined_max_value DESC (highest value first)
    - Header contains: tier, l3_name, l1_name, l2_name, lead_archetype, quick_win, combined_max_value, ai_suitability_summary, flag_count, flags
    - Null lead_archetype renders as empty cell, not "null"
    - Flags array joins with "; " separator
  </behavior>
  <action>
    1. Create src/types/scoring.ts with these types (contract with Phase 4):
       - ConfidenceLevel = "HIGH" | "MEDIUM" | "LOW"
       - SubDimensionScore: { score: number (0-3), reason: string }
       - TechnicalFeasibility: { data_readiness, platform_fit, archetype_confidence: SubDimensionScore, total: number (0-9), confidence: ConfidenceLevel }
       - AdoptionRealism: { decision_density, financial_gravity, impact_proximity, confidence_signal: SubDimensionScore, total: number (0-12), confidence: ConfidenceLevel }
       - ValueEfficiency: { value_density, simulation_viability: SubDimensionScore, total: number (0-6), confidence: ConfidenceLevel }
       - ScoredOpportunity: { l3_name, l1_name, l2_name, lead_archetype: LeadArchetype | null, technical: TechnicalFeasibility, adoption: AdoptionRealism, value: ValueEfficiency, composite: number (0.0-1.0), overall_confidence: ConfidenceLevel, promotes_to_simulation: boolean }

    2. Create src/types/triage.ts with these types (contract with Phase 3):
       - Tier = 1 | 2 | 3
       - RedFlagType = "dead_zone" | "no_stakes" | "confidence_gap" | "phantom" | "orphan"
       - RedFlag: { type: RedFlagType, reason: string }
       - TriagedOpportunity: { tier: Tier, l3_name: string, l1_name: string, l2_name: string, lead_archetype: LeadArchetype | null, quick_win: boolean, combined_max_value: number | null, ai_suitability_summary: string | null, flags: RedFlag[], skipped: boolean, composite: number | null }

    3. Create src/output/tsv-utils.ts with tsvCell() and tsvRow() helpers as described in research.

    4. Create src/output/format-triage-tsv.ts: pure function taking TriagedOpportunity[] and returning string. Sort by tier ASC, then combined_max_value DESC within tier. Use tsvRow for each row. Include header. End with newline.

    Use import type where possible. Import LeadArchetype from "../types/hierarchy.js".
  </action>
  <verify>
    <automated>npx tsx --test src/output/format-triage-tsv.test.ts</automated>
  </verify>
  <done>
    - src/types/scoring.ts exports all scoring types
    - src/types/triage.ts exports all triage types
    - tsvCell handles null, undefined, boolean, number, string correctly
    - formatTriageTsv produces tier-sorted TSV with correct header and trailing newline
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement feasibility scores TSV formatter</name>
  <files>src/output/format-scores-tsv.ts, src/output/format-scores-tsv.test.ts</files>
  <behavior>
    - formatScoresTsv([]) returns header row + trailing newline only
    - Header contains all 19 columns: l3_name, l1_name, l2_name, archetype, data_readiness, platform_fit, archetype_conf, tech_total, decision_density, financial_gravity, impact_proximity, confidence_signal, adoption_total, value_density, simulation_viability, value_total, composite, confidence, promotes_to_sim
    - Scores sorted by composite DESC (highest first)
    - composite formatted to 2 decimal places
    - promotes_to_simulation renders as "Y" or "N"
    - Null archetype renders as empty cell
    - Sub-dimension score columns contain the numeric score only (not the reason)
    - With 2 scored opportunities, higher composite appears first
  </behavior>
  <action>
    Create src/output/format-scores-tsv.ts: pure function taking ScoredOpportunity[] and returning string. 19 columns as specified in research (see "Feasibility Scores TSV Columns" section). Sort by composite DESC. Use tsvRow from tsv-utils. Format composite with toFixed(2). End with newline.
  </action>
  <verify>
    <automated>npx tsx --test src/output/format-scores-tsv.test.ts</automated>
  </verify>
  <done>
    - formatScoresTsv produces a 19-column TSV with correct header
    - Rows sorted by composite descending
    - All sub-dimension scores rendered as numbers
    - Composite formatted to 2 decimal places
    - All tests pass
  </done>
</task>

</tasks>

<verification>
npx tsx --test src/output/format-triage-tsv.test.ts src/output/format-scores-tsv.test.ts
</verification>

<success_criteria>
- src/types/scoring.ts and src/types/triage.ts define the complete cross-phase type contracts
- formatTriageTsv produces tier-sorted TSV with all required columns and correct null handling
- formatScoresTsv produces composite-sorted TSV with all 9 sub-dimensions, lens totals, and composite
- All tests green with npx tsx --test
</success_criteria>

<output>
After completion, create `.planning/phases/05-scoring-output/05-01-SUMMARY.md`
</output>
