---
phase: 05-scoring-output
plan: 03
type: tdd
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/output/write-evaluation.ts
  - src/output/write-evaluation.test.ts
autonomous: true
requirements:
  - SCOR-07
  - SCOR-08
  - OUTP-01
  - OUTP-02
  - OUTP-03
  - OUTP-04

must_haves:
  truths:
    - "writeEvaluation creates evaluation/ directory and writes all 4 files"
    - "Written triage.tsv matches formatTriageTsv output"
    - "Written feasibility-scores.tsv matches formatScoresTsv output"
    - "Written adoption-risk.md matches formatAdoptionRisk output"
    - "Written tier1-report.md matches formatTier1Report output"
    - "Function returns success with list of written file paths"
  artifacts:
    - path: "src/output/write-evaluation.ts"
      provides: "writeEvaluation orchestrator function"
      exports: ["writeEvaluation"]
  key_links:
    - from: "src/output/write-evaluation.ts"
      to: "src/output/format-triage-tsv.ts"
      via: "import formatTriageTsv"
      pattern: "import.*formatTriageTsv.*from.*format-triage-tsv"
    - from: "src/output/write-evaluation.ts"
      to: "src/output/format-scores-tsv.ts"
      via: "import formatScoresTsv"
      pattern: "import.*formatScoresTsv.*from.*format-scores-tsv"
    - from: "src/output/write-evaluation.ts"
      to: "src/output/format-adoption-risk.ts"
      via: "import formatAdoptionRisk"
      pattern: "import.*formatAdoptionRisk.*from.*format-adoption-risk"
    - from: "src/output/write-evaluation.ts"
      to: "src/output/format-tier1-report.ts"
      via: "import formatTier1Report"
      pattern: "import.*formatTier1Report.*from.*format-tier1-report"
    - from: "src/output/write-evaluation.ts"
      to: "node:fs/promises"
      via: "fs.mkdir + fs.writeFile"
      pattern: "fs\\.(mkdir|writeFile)"
---

<objective>
Wire all four formatters into a single writeEvaluation orchestrator that creates the evaluation/ directory and writes all output files.

Purpose: Provide a single entry point for the pipeline to produce all Phase 5 output artifacts. This is the integration point that Phase 7 (pipeline orchestration) will call.
Output: writeEvaluation function that creates evaluation/ with triage.tsv, feasibility-scores.tsv, adoption-risk.md, and tier1-report.md.
</objective>

<execution_context>
@/Users/vincent.wicker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincent.wicker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-scoring-output/05-RESEARCH.md
@.planning/phases/05-scoring-output/05-01-SUMMARY.md
@.planning/phases/05-scoring-output/05-02-SUMMARY.md
</context>

<interfaces>
<!-- Imports from Plan 01 outputs -->

From src/output/format-triage-tsv.ts:
```typescript
export function formatTriageTsv(opportunities: TriagedOpportunity[]): string;
```

From src/output/format-scores-tsv.ts:
```typescript
export function formatScoresTsv(opportunities: ScoredOpportunity[]): string;
```

<!-- Imports from Plan 02 outputs -->

From src/output/format-adoption-risk.ts:
```typescript
export function formatAdoptionRisk(opportunities: TriagedOpportunity[], date?: string): string;
```

From src/output/format-tier1-report.ts:
```typescript
export function formatTier1Report(scored: ScoredOpportunity[], tier1Names: Set<string>, companyName: string, date?: string): string;
```

From src/types/scoring.ts:
```typescript
export interface ScoredOpportunity { /* ... */ }
```

From src/types/triage.ts:
```typescript
export interface TriagedOpportunity { /* ... */ }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement write-evaluation orchestrator</name>
  <files>src/output/write-evaluation.ts, src/output/write-evaluation.test.ts</files>
  <behavior>
    - writeEvaluation creates evaluation/ subdirectory under outputDir
    - writeEvaluation writes exactly 4 files: triage.tsv, feasibility-scores.tsv, adoption-risk.md, tier1-report.md
    - Returns { success: true, files: string[] } with absolute paths to all 4 files
    - Returns { success: false, error: string } if directory creation or file write fails
    - Creates directory with { recursive: true } so parent dirs are created if needed
    - Each file content matches the corresponding formatter output exactly
    - Works with empty arrays (no opportunities) -- still creates all 4 files
    - Tier 1 names derived from triage data (tier === 1) for passing to formatTier1Report
  </behavior>
  <action>
    Create src/output/write-evaluation.ts with:

    ```
    export async function writeEvaluation(
      outputDir: string,
      scoredOpportunities: ScoredOpportunity[],
      triagedOpportunities: TriagedOpportunity[],
      companyName: string
    ): Promise<{ success: true; files: string[] } | { success: false; error: string }>
    ```

    Implementation:
    1. Create evaluation/ dir: `await fs.mkdir(path.join(outputDir, "evaluation"), { recursive: true })`
    2. Derive tier1Names: `new Set(triagedOpportunities.filter(o => o.tier === 1).map(o => o.l3_name))`
    3. Call all 4 formatters to get content strings
    4. Write all 4 files using fs.writeFile with "utf-8" encoding
    5. Wrap in try/catch -- on error return { success: false, error: err.message }
    6. On success return { success: true, files: [...paths] }

    Follow project error handling pattern: return Result-style objects, never throw.

    For testing: use node:fs/promises to create a temp directory (os.tmpdir()), run writeEvaluation, verify files exist and content matches, clean up. Use test fixtures with 2-3 opportunities covering different tiers and flag types.
  </action>
  <verify>
    <automated>npx tsx --test src/output/write-evaluation.test.ts</automated>
  </verify>
  <done>
    - writeEvaluation creates evaluation/ directory and writes all 4 output files
    - File contents match formatter outputs exactly
    - Returns success with file paths on success
    - Returns error on failure without throwing
    - Integration test verifies files on disk
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and verify all output modules</name>
  <files>src/output/write-evaluation.test.ts</files>
  <action>
    Run the complete test suite for all Phase 5 output modules:
    1. `npx tsx --test src/output/*.test.ts` -- all 5 test files must pass
    2. `npx tsx --test src/**/*.test.ts` -- full project suite must pass (no regressions)
    3. Verify that src/output/ contains exactly these files: tsv-utils.ts, format-triage-tsv.ts, format-scores-tsv.ts, format-adoption-risk.ts, format-tier1-report.ts, write-evaluation.ts, and their corresponding .test.ts files
    4. Verify that src/types/ contains scoring.ts and triage.ts with all expected exports

    If any test fails, fix the issue in the relevant module. Do not modify test expectations unless they contain a bug.
  </action>
  <verify>
    <automated>npx tsx --test src/output/*.test.ts && npx tsx --test src/**/*.test.ts</automated>
  </verify>
  <done>
    - All 5 output test files pass (format-triage-tsv, format-scores-tsv, format-adoption-risk, format-tier1-report, write-evaluation)
    - Full project test suite passes with no regressions
    - All Phase 5 files present in src/output/ and src/types/
  </done>
</task>

</tasks>

<verification>
npx tsx --test src/output/*.test.ts && npx tsx --test src/**/*.test.ts
</verification>

<success_criteria>
- writeEvaluation creates evaluation/ directory with all 4 required files
- Integration test verifies file creation and content correctness on disk
- Full project test suite passes with zero regressions
- Phase 5 delivers all 6 requirements: SCOR-07, SCOR-08, OUTP-01, OUTP-02, OUTP-03, OUTP-04
</success_criteria>

<output>
After completion, create `.planning/phases/05-scoring-output/05-03-SUMMARY.md`
</output>
