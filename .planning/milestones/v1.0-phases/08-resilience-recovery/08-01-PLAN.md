---
phase: 08-resilience-recovery
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/infra/retry-policy.ts
  - src/infra/retry-policy.test.ts
autonomous: true
requirements:
  - INFR-01
must_haves:
  truths:
    - "Engine recovers from primary LLM call failure by trying a fallback prompt"
    - "Engine skips and logs when both primary and fallback fail without crashing"
    - "Caller receives structured result indicating which tier resolved (primary, fallback, skipped)"
  artifacts:
    - path: "src/infra/retry-policy.ts"
      provides: "Three-tier resilient LLM call wrapper"
      exports: ["callWithResilience", "RetryPolicyOptions", "ResilientResult"]
    - path: "src/infra/retry-policy.test.ts"
      provides: "Unit tests for retry-policy"
      min_lines: 40
  key_links:
    - from: "src/infra/retry-policy.ts"
      to: "src/scoring/ollama-client.ts"
      via: "imports scoreWithRetry"
      pattern: "import.*scoreWithRetry.*ollama-client"
---

<objective>
Implement three-tier resilient LLM call wrapper: primary retry (existing scoreWithRetry) -> fallback prompt -> skip-and-log.

Purpose: INFR-01 requires graceful recovery from individual LLM call failures. The existing scoreWithRetry handles retry with exponential backoff; this module adds fallback prompt and skip-and-log tiers on top.
Output: src/infra/retry-policy.ts with callWithResilience function, fully tested.
</objective>

<execution_context>
@/Users/vincent.wicker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincent.wicker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-resilience-recovery/08-RESEARCH.md

<interfaces>
<!-- Existing interfaces this plan depends on -->

From src/scoring/ollama-client.ts:
```typescript
export type ValidatedResult<T> =
  | { success: true; data: T }
  | { success: false; error: string };

export async function scoreWithRetry<T>(
  schema: ZodSchema<T>,
  callFn: () => Promise<string>,
  maxRetries: number = 3,
): Promise<ValidatedResult<T>>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement callWithResilience with TDD</name>
  <files>src/infra/retry-policy.ts, src/infra/retry-policy.test.ts</files>
  <behavior>
    - Test 1: Returns primary result with resolvedVia="primary" when primaryCall succeeds on first try
    - Test 2: Falls back to fallbackCall with resolvedVia="fallback" when primary exhausts retries
    - Test 3: Returns resolvedVia="skipped" with success=false when both primary and fallback fail
    - Test 4: Returns resolvedVia="skipped" when primary fails and no fallbackCall provided
    - Test 5: Passes maxRetries through to scoreWithRetry (verify retry count)
    - Test 6: skipReason contains the original error message from primary failure
  </behavior>
  <action>
RED: Create src/infra/retry-policy.test.ts with all 6 test cases using a simple Zod schema (z.object({ score: z.number() })). Mock primaryCall and fallbackCall as async functions returning JSON strings or throwing errors. Use node:test describe/it and assert/strict. Run tests -- all must FAIL.

GREEN: Create src/infra/retry-policy.ts implementing:
- RetryPolicyOptions<T> interface with: primaryCall (required), fallbackCall (optional), schema (ZodSchema<T>), maxRetries (default 3), label (string for logging context)
- ResilientResult<T> type with: result (ValidatedResult<T>), resolvedVia ("primary" | "fallback" | "skipped"), skipReason (optional string)
- callWithResilience<T> async function that:
  1. Calls scoreWithRetry with primaryCall and schema
  2. If primary succeeds, returns with resolvedVia="primary"
  3. If primary fails and fallbackCall exists, calls scoreWithRetry with fallbackCall
  4. If fallback succeeds, returns with resolvedVia="fallback"
  5. Otherwise returns success=false with resolvedVia="skipped" and skipReason from primary error

Import scoreWithRetry from "../scoring/ollama-client.js". Use project convention: camelCase functions, Result type pattern, no thrown exceptions.

REFACTOR: Ensure exports are clean. Run tests again -- all must PASS.
  </action>
  <verify>
    <automated>cd /Users/vincent.wicker/Documents/agent-factory/src && npx tsx --test infra/retry-policy.test.ts</automated>
  </verify>
  <done>callWithResilience passes all 6 tests covering primary success, fallback success, skip-and-log, no fallback provided, retry count passthrough, and skipReason propagation</done>
</task>

</tasks>

<verification>
cd /Users/vincent.wicker/Documents/agent-factory/src && npm test
All existing tests still pass. retry-policy tests pass.
</verification>

<success_criteria>
- callWithResilience exported from src/infra/retry-policy.ts
- 6 unit tests passing covering all three tiers of failure handling
- No new npm dependencies added
- Existing test suite unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/08-resilience-recovery/08-01-SUMMARY.md`
</output>
