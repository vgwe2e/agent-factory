---
phase: 01-project-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/package.json
  - src/tsconfig.json
  - src/types/hierarchy.ts
  - src/schemas/hierarchy.ts
autonomous: true
requirements:
  - INGST-02
must_haves:
  truths:
    - "TypeScript project compiles with strict mode"
    - "Zod schemas validate the hierarchy JSON export structure"
    - "Zod schemas reject malformed exports with clear error paths"
  artifacts:
    - path: "src/package.json"
      provides: "Project manifest with zod, typescript, commander dependencies"
      contains: "zod"
    - path: "src/tsconfig.json"
      provides: "Strict TypeScript configuration targeting ES2022 + Node"
      contains: "strict"
    - path: "src/types/hierarchy.ts"
      provides: "TypeScript types for the full hierarchy export"
      exports: ["HierarchyExport", "CompanyContext", "L4Activity", "L3Opportunity", "Meta"]
    - path: "src/schemas/hierarchy.ts"
      provides: "Zod schemas that validate hierarchy JSON and produce typed output"
      exports: ["hierarchyExportSchema", "companyContextSchema", "l4ActivitySchema", "l3OpportunitySchema"]
  key_links:
    - from: "src/schemas/hierarchy.ts"
      to: "src/types/hierarchy.ts"
      via: "z.infer exports matching TypeScript types"
      pattern: "z\\.infer<typeof"
---

<objective>
Scaffold the aera-evaluate CLI project and define Zod schemas for hierarchy JSON validation.

Purpose: Establish the TypeScript project with strict typing and Zod validation schemas that all downstream ingestion code builds on. The schemas enforce the hierarchy export contract so malformed files are rejected early with clear errors.

Output: Compilable TypeScript project in src/ with types and Zod schemas for the full hierarchy export structure.
</objective>

<execution_context>
@/Users/vincent.wicker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincent.wicker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

The hierarchy JSON export has this top-level structure (from ford_hierarchy_v2_export.json):
- `meta`: { project_name: string, version_date: string, created_date: string, exported_by: string|null, description: string }
- `company_context`: { industry: string, company_name: string, annual_revenue: number|null, cogs: number|null, sga: number|null, ebitda: number|null, working_capital: number|null, inventory_value: number|null, annual_hires: number|null, employee_count: number|null, geographic_scope: string, notes: string, business_exclusions: string, enterprise_applications: string[], detected_applications: string[], pptx_template: any|null, industry_specifics: any|null, raw_context: string, enriched_context: object, enrichment_applied_at: string, existing_systems: any[], hard_exclusions: any[], filtered_skills: object[] }
- `hierarchy`: L4Activity[] (2016 items) -- each has: { id: string, name: string, description: string, l1: string, l2: string, l3: string, financial_rating: string, value_metric: string, impact_order: string, rating_confidence: string, ai_suitability: string, decision_exists: boolean, decision_articulation: string, escalation_flag: string|null, skills: any[] }
- `l3_opportunities`: L3Opportunity[] (362 items) -- each has: { l3_name: string, l2_name: string, l1_name: string, opportunity_exists: boolean, opportunity_name: string, opportunity_summary: string, lead_archetype: string, supporting_archetypes: string[], combined_max_value: number, implementation_complexity: string, quick_win: boolean, competitive_positioning: string|null, aera_differentiators: string[], l4_count: number, high_value_l4_count: number, rationale: string }
- Other top-level keys (industry_analysis, domain_references, program_focuses, etc.) exist but are not needed for Phase 1 ingestion. Schema should accept and passthrough these fields.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold TypeScript project with dependencies</name>
  <files>src/package.json, src/tsconfig.json</files>
  <behavior>
    - Test: `cd src && npm install` completes without errors
    - Test: `cd src && npx tsc --noEmit` compiles with zero errors (once source files exist)
  </behavior>
  <action>
Create `src/` directory at project root (alongside existing `seed/` directory -- do NOT modify seed/).

Create `src/package.json`:
- name: "aera-evaluate"
- version: "0.1.0"
- type: "module" (ESM)
- bin: { "aera-evaluate": "./dist/cli.js" }
- scripts: { "build": "tsc", "dev": "tsx cli.ts", "test": "node --test" }
- dependencies: zod (^3.24), commander (^13), tsx (for dev)
- devDependencies: typescript (^5.7), @types/node (^22)

Create `src/tsconfig.json`:
- target: "ES2022"
- module: "NodeNext" / moduleResolution: "NodeNext"
- strict: true
- outDir: "./dist"
- rootDir: "."
- declaration: true
- esModuleInterop: true
- skipLibCheck: true
- resolveJsonModule: true

Run `cd src && npm install` to generate lockfile.
  </action>
  <verify>
    <automated>cd /Users/vincent.wicker/Documents/agent-factory/src && npm install && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>src/ directory exists with package.json, tsconfig.json, node_modules installed, tsc compiles cleanly</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define TypeScript types and Zod schemas for hierarchy export</name>
  <files>src/types/hierarchy.ts, src/schemas/hierarchy.ts, src/schemas/hierarchy.test.ts</files>
  <behavior>
    - Test: Valid hierarchy JSON (meta + company_context + hierarchy[] + l3_opportunities[]) parses successfully and returns typed object
    - Test: Missing required field (e.g., no meta.project_name) returns ZodError with clear path
    - Test: Empty hierarchy array is valid (no activities is a valid export)
    - Test: L4 with invalid financial_rating enum value is rejected
    - Test: L3 with missing lead_archetype is rejected
    - Test: company_context with null numeric fields (annual_revenue: null) passes validation
  </behavior>
  <action>
Create `src/types/hierarchy.ts` with TypeScript interfaces derived from the JSON schema documented in context:
- Meta, CompanyContext (with EnrichedContext sub-type), L4Activity, L3Opportunity, HierarchyExport
- Use string literal unions for known enums: financial_rating ("HIGH"|"MEDIUM"|"LOW"), ai_suitability ("HIGH"|"MEDIUM"|"LOW"|"NONE"), impact_order ("FIRST"|"SECOND"|"THIRD"), rating_confidence ("HIGH"|"MEDIUM"|"LOW"), lead_archetype ("DETERMINISTIC"|"AGENTIC"|"GENERATIVE"), implementation_complexity ("LOW"|"MEDIUM"|"HIGH")

Create `src/schemas/hierarchy.ts` with Zod schemas that validate and produce these types:
- metaSchema, companyContextSchema, l4ActivitySchema, l3OpportunitySchema, hierarchyExportSchema
- Use z.enum() for the string literal unions above
- Use z.number().nullable() for financial fields that can be null
- Use z.string().nullable() for optional string fields (exported_by, escalation_flag, competitive_positioning)
- hierarchyExportSchema must use .passthrough() at the top level to accept extra keys (industry_analysis, domain_references, etc.) without stripping them
- Export z.infer types alongside schemas so consumers can use either

Create `src/schemas/hierarchy.test.ts` using Node.js built-in test runner (node:test + node:assert):
- Write tests for all behaviors listed above
- Use minimal valid fixtures (not the full 2.5MB file) -- construct small objects matching the schema
- Test that ZodError.issues[0].path contains the field path for debugging

Run tests RED first, then ensure schemas pass them GREEN.
  </action>
  <verify>
    <automated>cd /Users/vincent.wicker/Documents/agent-factory/src && node --test schemas/hierarchy.test.ts 2>&1</automated>
  </verify>
  <done>All schema tests pass. Valid exports parse to typed objects. Invalid exports produce ZodErrors with field paths. Enums reject unknown values.</done>
</task>

</tasks>

<verification>
- `cd src && npx tsc --noEmit` compiles with zero errors
- `cd src && node --test schemas/hierarchy.test.ts` all tests pass
- Zod schema correctly validates a minimal hierarchy export fixture
- Zod schema rejects exports missing required fields with clear path in error
</verification>

<success_criteria>
- TypeScript project in src/ compiles cleanly with strict mode
- Zod schemas cover meta, company_context, hierarchy (L4), and l3_opportunities
- Schema tests prove both valid and invalid inputs are handled correctly
- Types and schemas are exported for downstream plan consumption
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-01-SUMMARY.md`
</output>
