# Auto-generated by /audit-prompt. Edit as needed.
project:
  name: "Aera Skill Feasibility Engine"
  domain: "Supply chain / enterprise decision intelligence"
  product_values:
    - "Scoring accuracy — scores must discriminate between opportunities with meaningful variance"
    - "Evidence-grounded — every score must be justified by data signals present in the input"
    - "Conservative realism — prefer under-scoring to over-scoring; value lens uses intentional caps on combined_max_value"

paths:
  prompt_dir: "src/scoring/prompts"
  audit_logs: "audit/logs"
  audit_tests: "audit/tests"
  intent_manifest: null

prompt_inventory:
  - file: "src/scoring/prompts/technical.ts"
    prompt_id: "technical-feasibility"
    description: "Scores data_readiness, aera_platform_fit, archetype_confidence (0-3 each, max 9). Uses Aera knowledge context."
  - file: "src/scoring/prompts/adoption.ts"
    prompt_id: "adoption-realism"
    description: "Scores decision_density, financial_gravity, impact_proximity, confidence_signal (0-3 each, max 12)."
  - file: "src/scoring/prompts/value.ts"
    prompt_id: "value-efficiency"
    description: "Scores value_density, simulation_viability (0-3 each, max 6). Input values are intentionally capped."

failure_patterns:
  - id: "FP-P001"
    name: "Collapsed platform_fit"
    description: "aera_platform_fit scored 0 for all 322 opportunities in vLLM evaluation"
    signal: "100% of scores at floor value for a sub-dimension"
    fix: "Knowledge context lacked capability-level grounding; enriched with platform-capabilities.json, use-case-mappings.json, capability-keywords.json"
  - id: "FP-P002"
    name: "Ceiling clustering on adoption sub-dimensions"
    description: "decision_density and financial_gravity cluster at 3 for majority of scored opportunities"
    signal: ">70% of scores at max value for a sub-dimension"
    fix: "Tighten rubric thresholds or add discriminating criteria between score 2 and 3"
